import { Div, ImageCard } from 'components/atoms';
import React, { FC } from 'react';
import { Props } from './props';

export const Sun: FC<Props> = ({ ...rest }: Props) => (
    <Div>
        <ImageCard

        />
    </Div>
);